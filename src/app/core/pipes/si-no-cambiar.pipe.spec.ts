import { SiNoCambiarPipe } from './si-no-cambiar.pipe';

describe('SiNoCambiarPipe', () => {
  it('create an instance', () => {
    const pipe = new SiNoCambiarPipe();
    expect(pipe).toBeTruthy();
  });
});
